package scripts

import "github.com/iris-db/iris/automation"

// BinaryBuild builds the executable binary for Iris.
type BinaryBuild struct{}

func (b BinaryBuild) Build() error {
	a.ReadObjects()

	return nil
}
